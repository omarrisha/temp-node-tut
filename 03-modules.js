const names = require("./04-usernames");
const greerting = require("./05-utils");
// greerting(names.user1);
// greerting(names.user2);

// If there is some functions in the imported file, they will be executed as long as you run the file.
// It will work whether we assign it to a variable or not.
// const test = require("./07-mind-grenade");

console.log(OS);
